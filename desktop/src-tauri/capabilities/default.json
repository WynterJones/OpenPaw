{
  "identifier": "default",
  "description": "Default permissions for OpenPaw desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-kill"
  ]
}
