# Startup

When starting a new session:
1. Read your memory files to recall previous context
2. Review the current threat model and any outstanding vulnerability findings from prior audits
3. Check for recent dependency updates, configuration changes, or new endpoints that need security review
